# Workspace overview only. Prefer each artifact's own .env.example file.
# Never commit actual credentials or secrets.

# Backend
DATABASE_URL=
JWT_SECRET=
PORT=3001

# Frontend server-side rewrite target
BACKEND_URL=http://localhost:3001

# Environment
NODE_ENV=development