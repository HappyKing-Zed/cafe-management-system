export enum Role {
  ADMIN = 'admin',
  OWNER = 'owner',
  MANAGER = 'manager',
  COORDINATOR = 'coordinator',
  WAITER = 'waiter',
  CHEF = 'chef',
  CASHIER = 'cashier',
  STOREKEEPER = 'storekeeper',
}
