# NestJS server settings. Keep real values in Replit Secrets.

# Required PostgreSQL connection string. Leave blank in source control.
DATABASE_URL=

# Required JWT signing secret. Leave blank in source control.
JWT_SECRET=

# Replit supplies PORT to the managed workflow.
PORT=3001
NODE_ENV=development