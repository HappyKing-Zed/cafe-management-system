# Next.js server settings. Do not place database or JWT secrets here.

# Server-side target for the unchanged /backend/api browser rewrite.
BACKEND_URL=http://localhost:3001

# Replit supplies PORT and REPLIT_DEV_DOMAIN to the managed workflow.
PORT=3000
REPLIT_DEV_DOMAIN=