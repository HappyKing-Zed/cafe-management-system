# Memory Index

- [Next.js/NestJS routing](nextjs-artifact-routing.md) — preview needs the `artifacts/habesha` shim artifact; frontend calls backend via `/backend/api`, not `/api`; `[[ports]]` in .replit doesn't route the preview.
